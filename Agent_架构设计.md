# Agent 架构设计 —— 完整框架计划

## 1. 最终架构（经过多轮讨论确定）

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                              用户                                            │
│                   浏览器打开 Agent 工作台 UI                                   │
└──────────────────────────────────┬───────────────────────────────────────────┘
                                   │ HTTP
                                   ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│                         Agent 工作台 (上海服务器)                              │
│                                                                              │
│  ┌────────────────────┐    ┌──────────────────────────────────────────────┐ │
│  │  Web UI            │    │  LangGraph Agent (主控)                       │ │
│  │  (Gradio/自研)      │    │  LLM: 阿里百炼 API                            │ │
│  │                    │    │                                              │ │
│  │  · 对话式交互       │    │  ┌──────────┐  ┌──────────┐  ┌──────────┐   │ │
│  │  · 工具卡片展示     │◄──►│  │ 工具1     │  │ 工具2     │  │ 工具3     │   │ │
│  │  · 历史记录         │    │  │ PR 分析   │  │ 页面测试  │  │ 需求检索  │   │ │
│  │  · 可插拔工具       │    │  └────┬─────┘  └────┬─────┘  └────┬─────┘   │ │
│  └────────────────────┘    │       │              │              │         │ │
│                            │       ▼              ▼              ▼         │ │
│                            │  ┌───────────────────────────────────────┐   │ │
│                            │  │          工具执行层 (Python)           │   │ │
│                            │  │  · Playwright (浏览器)                 │   │ │
│                            │  │  · Dify API (知识库检索+上传)          │   │ │
│                            │  │  · Azure DevOps (PR diff)             │   │ │
│                            │  │  · 未来: MCP / 数据库 / API 测试       │   │ │
│                            │  └───────────────────────────────────────┘   │ │
│                            └──────────────────────────────────────────────┘ │
│                                                                              │
│  网络出站:                                                                    │
│  ├─→ *.test.autobestdevops.com (测试网站)                                    │
│  ├─→ dev.azure.com (PR diff)                                                │
│  ├─→ dify-test.uat.autobestdevops.com (知识库)                               │
│  └─→ 阿里百炼 API (LLM)                                                      │
└──────────────────────────────────────────────────────────────────────────────┘
```

## 2. 组件职责

| 组件 | 职责 | 技术选型 |
|---|---|---|
| **Web UI** | 用户交互界面，对话式操作 | Gradio / 自研前端 |
| **LangGraph Agent** | 主控：理解意图、调度工具、生成回复 | LangGraph + 阿里百炼 |
| **工具: PR 分析** | 下载 PR diff → 上传 Dify → 检索 → 生成测试计划 | 已有 `download_pr_diff.py` |
| **工具: 页面测试** | Playwright 打开页面 → 截图 → 验证 → 返回结果 | Playwright + Chromium |
| **工具: 需求检索** | 检索 Dify 两个知识库，返回相关需求 | Dify API |
| **工具: 知识库上传** | 上传文档到 Dify 知识库 | Dify API |
| **Dify** | 纯知识库引擎：存储 + 检索（不负责推理） | 已有 |
| **阿里百炼** | LLM API，Agent 的大脑 | 阿里云 API |

## 3. Dify 角色：从 Agent 平台退回到知识库引擎

```
之前想的:                          最终确定的:
┌─ Dify Agent ──────────┐         ┌─ Dify ──────────────┐
│ 推理 + 工具调用         │         │ 纯知识库             │
│ UI (Chatbot)           │    →    │ 存储 + 检索          │
│ 知识库                 │         │ 不负责推理           │
└───────────────────────┘         └──────────────────────┘
                                            ▲
                                    Dify API 调用
                                            │
                                  ┌─ LangGraph Agent ──┐
                                  │ 推理 + 工具调用      │
                                  │ UI (Agent 工作台)    │
                                  └─────────────────────┘
```

**原因**：Dify Agent 只能调用 HTTP 工具，无法执行脚本、Playwright、MCP 等本地操作。

## 4. 知识库（已知，待迁移）

| 知识库 | 内容 | 当前部署 | 目标部署 |
|---|---|---|---|
| 需求库 | 156 个产品需求 .md | 美国 Dify | 上海 Dify（或美国 Dify 通过 API 调用） |
| 需求变更库 | 每次 PR diff 增量追加 | 美国 Dify | 同上 |

> **待定**：Dify 知识库留在美国还是也在上海搭一套？影响 Agent 调 Dify API 的网络延迟。

## 5. 工具系统设计（可插拔）

```
工具注册 → Agent 加载 → LLM 理解 → 用户请求 → Agent 匹配工具 → 执行 → 返回结果
```

每个工具定义：
```python
{
    "name": "analyze_pr",
    "description": "分析 Azure DevOps PR，下载 diff，检索知识库，生成测试计划",
    "parameters": {
        "pr_url": "PR 链接"
    },
    "handler": analyze_pr_handler  # 实际执行函数
}
```

新增工具只需：
1. 写 handler 函数
2. 注册到工具列表
3. UI 自动展示

| 已规划工具 | 已规划工具 |
|---|---|
| 📋 **PR 分析** | 下载 diff → 上传 → 检索 → 生成测试 |
| 🧪 **页面测试** | Playwright 爬页面 → 截图 → 验证 |
| 📊 **需求检索** | 检索 Dify 知识库，返回上下文 |
| 🔮 **API 测试** | 测试 API 接口响应 |
| 🔮 **代码审查** | 审查代码变更 |
| 🔮 **数据库查询** | 查询测试数据库 |

## 6. 开发计划

```
Phase 1: 核心 Agent (当前)
├─ download_pr_diff.py  ✅ 已完成
├─ server.py            ⬜ FastAPI 服务
├─ test_runner.py       ⬜ Playwright 测试
├─ agent.py             ⬜ LangGraph Agent 主控
└─ 端到端跑通            ⬜ 命令行验证

Phase 2: Web UI
├─ ui.py                ⬜ Gradio 界面
├─ 对话式交互             ⬜
├─ 工具卡片展示           ⬜
└─ 历史记录               ⬜

Phase 3: 工具扩展
├─ API 测试工具           ⬜
├─ 数据库查询工具          ⬜
└─ MCP 接入               ⬜

Phase 4: 部署上线
├─ 上海服务器部署          ⬜
├─ systemd 自动拉起        ⬜
└─ 端到端验证              ⬜
```

## 7. 核心流程

### 7.1 PR 分析流程

```
用户: "分析 PR #29921"
    │
    ▼
Agent 接收消息 → LLM 理解意图 → 匹配工具: analyze_pr
    │
    ▼
analyze_pr 执行:
    ├─ Azure DevOps API: 下载 PR diff
    ├─ Dify API: 上传 diff 到需求变更库
    ├─ Dify API: 检索需求库 + 变更库
    └─ LLM: 生成测试计划
    │
    ▼
Agent 返回: 变更详情 + 测试计划（富卡片）
    │
    ▼
用户: "执行" / "执行第1-3项" / "修改..."
```

### 7.2 页面测试流程

```
用户: "执行测试"
    │
    ▼
Agent 接收消息 → LLM 理解意图 → 匹配工具: run_test
    │
    ▼
run_test 逐项执行:
    ├─ Playwright 打开 test 环境页面
    ├─ 截图
    ├─ 内容验证（比对预期）
    ├─ 返回 PASS/FAIL
    └─ 循环下一项
    │
    ▼
Agent 返回: 测试结果汇总 + 截图（富卡片）
```

## 8. 关键决策记录

| # | 决策 | 结论 |
|---|---|---|
| 1 | Agent 框架 | LangGraph（不挑模型，灵活性高） |
| 2 | LLM | 阿里百炼 API |
| 3 | Dify 角色 | 纯知识库引擎，不负责推理 |
| 4 | 知识库 | 需求库 + 需求变更库，两个库 |
| 5 | 测试环境 | 仅 test 环境，不碰生产 |
| 6 | 交互方式 | 对话式，新增工具可插拔 |
| 7 | 部署 | 上海服务器（待申请） |
| 8 | 浏览器 | Playwright + Chromium |

## 9. 待确认事项

| # | 事项 | 状态 |
|---|---|---|
| 1 | 上海服务器申请 | ⬜ 待申请 |
| 2 | Dify 知识库：留在美国 or 上海也搭？ | ⬜ 待定 |
| 3 | 阿里百炼 API Key | ⬜ 待获取 |
| 4 | test 环境完整域名列表 | ⬜ 待确认 |
| 5 | Azure DevOps PAT Token（长期有效的） | ⬜ 待获取 |